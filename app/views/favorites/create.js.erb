<!--alert("Hello")-->
<!--$(".favorite-btn").html("<%#= escape_javascript(render partial: 'favorites/favorite-btn', locals: {book: book}) %>");-->
<!--// インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用できます。-->
<!--// 以下の記述でも同じ挙動をします。html()以降では部分テンプレートを呼び出しています。-->
$('.table__favorite--'+<%= @book.id %>).html("<%= j(render 'favorite-btn', book: @favorite.book) %>");
<!--$(".favorite-btn").html("<%#= j(render 'favorites/favorite-btn', book: @book) %>");-->